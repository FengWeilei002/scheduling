# --- 1. 核心：忽略虚拟环境 (已生效，保持不动) ---
venvschedling/
venv/
env/
.env

# --- 2. 忽略 Python 自动生成的缓存 (必须加) ---
# 你的截图里暂时还没出现 __pycache__，但运行几次代码后一定会出现
__pycache__/
*.py[cod]

# --- 3. 忽略代码生成的图片/结果 ---
# 你现在的 day01_result.png 是绿色的(U)，意味着 Git 准备把这张图传上去。
# 通常我们不上传代码跑出来的临时结果图，建议忽略它：
*_result.png
*.jpg

# --- 4. 忽略 VS Code 配置文件 ---
.vscode/

# --- 5. 忽略系统垃圾文件 ---
.DS_Store
Thumbs.db